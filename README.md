# electron-cors-repro

## Project setup
```
npm install
```

### Runs the child window from `http://` source (fails)
```
npm run start:http
```

### Runs the child window from `file://` source (works)
```
npm run start:file
```
